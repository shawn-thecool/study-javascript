class StringInstrumentSpec {
  constructor({ price, maker, stringCount, woodType }) {
    this.price = price
    this.maker = maker
    this.stringCount = stringCount
    this.woodType = woodType;
  }
}

module.exports = StringInstrumentSpec
