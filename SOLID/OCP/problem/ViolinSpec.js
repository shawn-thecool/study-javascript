class ViolinSpec {
  constructor(props) {
    // 특정 정보군
    this.price = props.price
    this.maker = props.maker
    this.stringCount = props.stringCount
    this.woodType = props.woodType
  }
}

module.exports = ViolinSpec
